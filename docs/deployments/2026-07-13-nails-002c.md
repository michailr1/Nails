# NAILS-002C production installation

Дата: **13 июля 2026 года**  
Host: `de.funti.cc`

## Release state

Repository checkout after fast-forward:

```text
HEAD = ae761e5042e2af4685df7bdb1de9485e96bdac74
working tree = clean
```

Runtime plugin commit included in this checkout:

```text
d8264266256f6fc2c53b6eebd3b9bb6bbc722f7c
```

The three commits after `d826426…` changed documentation only.

Backend containers were not rebuilt. Existing NAILS-002B runtime remained healthy:

```text
backend release code = 40b25ff5fe519eda8602d0eeac7d06a1b191138d
Alembic = 0002 (head)
```

## Backup

Hermes profile backup:

```text
/root/.hermes/profiles/nails/backups/nails-002c-20260713T092318Z
```

Mode:

```text
700 root:root
```

Backup included existing profile configuration, environment and SOUL before installation.

## Plugin installation

Source:

```text
/opt/nails/repo/hermes/plugins/nails_onboarding
```

Target:

```text
/root/.hermes/profiles/nails/plugins/nails_onboarding
```

Checks:

- source and target file hashes matched;
- target directory mode: `700 root:root`;
- plugin discovered as user plugin;
- plugin `nails-onboarding` version `0.1.0` enabled;
- static registration exposed exactly one tool `nails_onboarding`;
- model-visible schema contains only `action`, `section`, `payload`.

## Profile environment

`NAILS_INTERNAL_API_KEY` was synchronized from the backend runtime setting into the profile environment.

Verified without printing values:

```text
KEYS_MATCH=true
LENGTH_OK=true
```

Profile `.env` remained:

```text
600 root:root
```

Bot token and Telegram allowlist were not changed.

## SOUL

Repository and production SOUL hashes matched after installation.

The deployed SOUL requires:

- no success claim without `ok=true`;
- explicit distinction between draft, confirmed onboarding block and active working data;
- no claim that schedule/services/bookings are active before materialization;
- no request for Telegram ID, tokens or technical secrets.

## Telegram tool configuration

`hermes config check`: OK.

Enabled Telegram tools exactly:

```text
clarify
image_gen
nails_onboarding
skills
tts
vision
```

No enabled:

```text
terminal
file
code_execution
web
browser
memory
session_search
delegation
cronjob
computer_use
context_engine
todo
kanban
MCP
GitHub
SSH
deploy tools
```

No generic HTTP tool was enabled.

## Gateway restart

Only the `nails` gateway was restarted.

```text
old PID = 991980
new PID = 1677495
state = active (running)
active since = Mon 2026-07-13 12:57:53 MSK
```

Recent logs showed no:

- traceback;
- plugin import error;
- manifest error;
- missing environment error;
- Telegram polling conflict;
- secret leak.

Default Hermes profile was not active and was not changed.

## Backend health

```json
{"status":"ok"}
```

```json
{"status":"ready"}
```

Backend containers were not rebuilt or restarted for this release.

## Production users

Two existing allowlisted Telegram users were provisioned in the backend without publishing their IDs:

- one role `admin`;
- one role `master`.

Telegram allowlist itself was not modified.

## Synthetic plugin-to-backend smoke

Result:

```text
NAILS_002C_PLUGIN_SMOKE_OK
```

Verified:

- non-Telegram context failed closed;
- model-supplied `telegram_user_id` was rejected as `invalid_arguments` before HTTP access;
- unknown and inactive users produced identical `access_denied` responses;
- two synthetic users received separate onboarding states and different drafts;
- plugin reached the backend only through fixed loopback URL;
- no generic URL or headers were model-controlled;
- backend remained healthy.

Synthetic cleanup:

```text
0|0|0
```

Temporary SQL and smoke files were removed.

## Side-effect check

Docker daemon:

```text
MainPID = 1052
state = active
```

Docker daemon was not restarted.

Amnezia containers retained the same IDs, `StartedAt` and running state:

- `amnezia-awg2`;
- `amnezia-dns`;
- `amnezia-xray`.

Not changed:

- default Hermes profile;
- Telegram bot token;
- Telegram allowlist;
- backend code and migrations;
- Nails containers;
- Nginx/Traefik;
- other Docker projects.

## Result

Infrastructure installation and synthetic production checks passed.

Current status:

```text
READY_FOR_TELEGRAM_NAILS_002C_TEST
```

NAILS-002C is not closed until the final two-account Telegram acceptance verifies real gateway context, cross-user isolation and pause/resume after a gateway restart.
