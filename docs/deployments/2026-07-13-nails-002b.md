# NAILS-002B production deployment

Дата: **13 июля 2026 года**  
Host: `de.funti.cc`

## Release

```text
Previous commit: cca0109ea8c716fdf03d97c34a1c0f06bfb5fc50
Deployed commit: 40b25ff5fe519eda8602d0eeac7d06a1b191138d
Alembic before: 0001 (head)
Alembic after: 0002 (head)
```

Repository:

```text
/opt/nails/repo
```

Working tree после deployment: clean.

## Backup

Перед migration создан PostgreSQL custom-format backup:

```text
/opt/nails/backups/nails_before_0002_20260712T224019Z.dump
```

- size: `26141` bytes;
- `pg_restore --list`: OK;
- backup не публиковался и не добавлялся в GitHub.

## Production environment

```text
/opt/nails/.env
```

- owner/mode: `root:root`, `600`;
- создан backup environment file;
- добавлена отдельная production authentication setting;
- значение не выводилось и не фиксировалось в отчётах.

## Containers

```text
nails-api — healthy, 127.0.0.1:8210 → 8000
nails-db  — healthy, no published port
```

Compose validation: OK. Services: только `nails-api`, `nails-db`.

## Health and migration

```json
{"status":"ok"}
```

```json
{"status":"ready"}
```

- Alembic current: `0002 (head)`;
- Alembic heads: `0002 (head)`;
- repeated `alembic upgrade head`: OK;
- columns confirmed: `confirmed_payload`, `confirmed_revision`, `revision`.

## Isolation and hardening

Database application role:

```text
SUPERUSER|CREATEDB|CREATEROLE|REPLICATION = 0|0|0|0
current_user = nails_app
```

API:

```text
bind = 127.0.0.1:8210
user = nails
readonly rootfs = true
cap_drop = ALL
no-new-privileges = true
```

PostgreSQL host port: absent.

## Synthetic production smoke

Result:

```text
NAILS_002B_SMOKE_OK
```

Authorization:

- missing authentication → `401`;
- wrong authentication → `401`;
- unknown synthetic user → `403`;
- inactive synthetic user → `403`;
- active synthetic admin starts onboarding.

Validation and workflow:

- invalid schedule → `422 invalid_onboarding_payload`;
- source payload values not echoed;
- services before schedule confirmation → `409 prior_sections_not_confirmed`;
- schedule revision 1 confirmation idempotent;
- revision 2 preserves previous effective payload until confirmation;
- services/buffers/bookings confirmation flow passes;
- unknown service in buffers → `409 unknown_service_reference`.

Persistence:

- onboarding paused;
- restarted only `nails-api`;
- state, drafts and confirmations survived;
- resume returned `in_progress`.

Completion and audit:

- complete returned `completed`, `current_step=null`;
- repeated complete idempotent;
- schedule revision 1 confirmation audit count: `1`;
- completion audit count: `1`;
- audit privacy leak count: `0`.

Cleanup:

```text
users|onboarding_states|audit_events = 0|0|0
```

Temporary smoke script removed.

## Logs

Checked API and DB tails:

- no traceback;
- no migration error;
- no authentication failure loop;
- no secret;
- no full payload;
- no synthetic contact values;
- restart counts remained `0` after final stabilization.

## Unrelated services

Docker daemon before/after:

```text
MainPID=1052
ActiveEnterTimestamp=Wed 2026-07-08 07:51:20 MSK
```

Docker daemon was not restarted.

Amnezia:

- `amnezia-awg2`: same container ID and `StartedAt`, running;
- `amnezia-dns`: same container ID and `StartedAt`, running;
- `amnezia-xray`: same container ID and `StartedAt`, running.

Not changed:

- Hermes;
- Telegram;
- Nginx/Traefik;
- other Docker projects;
- tracked repository files beyond fast-forward pull.

No prune, volume deletion or Docker daemon restart was performed.

## Result

`NAILS-002B` is accepted as **production-complete**.

The next active slice is `NAILS-002C`: a restricted Hermes onboarding domain tool with trusted Telegram identity and no generic HTTP access.
