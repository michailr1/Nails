# NAILS-002E4 V3 — successful production deployment

Date: **2026-07-13 21:58 UTC**.

Status: **deployment successful; rollback not required**.

## Release

```text
host: de.funti.cc
runbook: ops/deploy/nails-002e4-v3.sh
HEAD before: 5565a524b75a04fe5d8bc2c3e758d2994e9d9c12
HEAD after: 385a92962e3736553335d717adcdf4b83ac8a8b3
working tree: clean
success marker: NAILS_002E4_V3_DEPLOYMENT_OK
```

Backup:

```text
/root/.hermes/profiles/nails/backups/nails-002e4-v3-20260713T215800Z
```

## Preflight

Verified before mutation:

```text
CONFIG_PRESTATE_OK=true
ONBOARDING_PLUGIN_PRECHECK=true
KEYS_MATCH=true
```

The production checkout was on the expected baseline, backend health/readiness were green, the gateway was managed by root user-level systemd, and the installed Hermes version/import path matched the reviewed runtime contract.

## Repository and release validation

The repository was fast-forwarded from the expected baseline to the approved exact release SHA.

Static release validation:

```text
STATIC_RELEASE_VALIDATION=true
```

Only reviewed scheduling plugin files and the scheduling skill were eligible for runtime installation.

## Structured YAML update

The profile configuration was parsed as YAML, checked against the exact pre-state, changed atomically, parsed again and checked against the exact post-state.

```text
CONFIG_UPDATED_ATOMICALLY=true
CONFIG_POSTSTATE_OK=true
```

Enabled plugins after the change:

```text
nails-onboarding
nails-scheduling
```

Telegram toolset membership after the change:

```text
clarify
image_gen
nails_onboarding
nails_scheduling
skills
tts
vision
```

## Plugin and Telegram visibility verification

Before and after gateway start:

```text
PLUGIN_LIST_OK=true
PLUGIN_REGISTRY_OK=true
TELEGRAM_VISIBILITY_OK=true
KEYS_MATCH=true
```

Final runtime markers:

```text
plugin_discovery=idempotent
plugin_registry=ok
telegram_visibility=ok
gateway_error_scan=clean
```

A short startup journal contained zero literal mentions of the two Nails plugin names. This was accepted because plugin list, registry, generated Telegram definitions and error scan all passed.

## Gateway

```text
manager: root user-level systemd
fragment: /root/.config/systemd/user/hermes-gateway-nails.service
PID before: 2308856
controlled stop state: failed
PID after: 2318431
final state: active
```

The controlled stop state `failed` was accepted only because `MainPID=0`; the final state was required to be `active/running` with a new positive PID.

## Runtime file integrity

```text
plugin_source_target_match=true
skill_source_target_match=true
runtime_release_files_only=true
```

The scheduling runtime directory contained only the reviewed release files with the expected root-only ownership and modes.

## Backend and Docker isolation

```text
backend_api_container_unchanged=true
backend_db_container_unchanged=true
docker_daemon_unchanged=true
backend_health=ok
backend_ready=ok
migration_executed=false
database_write_executed=false
backend_restart_executed=false
```

The deployment changed only the repository checkout, scheduling plugin/skill runtime files, Hermes structured config and the Nails root user-level gateway process.

## Rollback

```text
rollback_performed=false
```

No rollback was needed.

## Preceding failed attempt

V3 was created after the safely rolled-back V2 attempt. The V2 defect and rollback evidence are recorded in:

[`2026-07-13-nails-002e4-v2-rollback.md`](2026-07-13-nails-002e4-v2-rollback.md).

The corrective rules retained by V3 are:

- use `discover_plugins()` without `force=True`;
- compare `_get_platform_tools(...)` as an unordered set by exact membership;
- sort only when passing a deterministic sequence to an API;
- include observed values in assertion failures.

## Remaining acceptance

Infrastructure deployment is complete, but issue #34 remains open until manual Telegram acceptance proves:

1. day view for 18 July 2026;
2. correct free slots for manicure;
3. exact client lookup and confirmed client creation;
4. confirmed booking creation;
5. idempotency/no duplicates;
6. invalid late slot rejection;
7. overlap rejection;
8. final read-only production counts and log privacy.

The exact continuation point is maintained in [`../context/current.md`](../context/current.md).
