name: nails-scheduling
version: 0.2.0
description: Restricted calendar and scheduling bridge from the Nails Hermes profile to the local Booking API
requires_env:
  - name: NAILS_INTERNAL_API_KEY
    description: Internal authentication key for the local Nails Booking API
    secret: true
provides_tools:
  - nails_scheduling
