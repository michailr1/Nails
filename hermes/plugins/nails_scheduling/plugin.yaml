name: nails-scheduling
version: 0.5.0
description: Restricted operational bridge from the Nails Hermes profile to the local scheduling API
requires_env:
  - name: NAILS_INTERNAL_API_KEY
    description: Internal authentication key for the local Nails scheduling API
    secret: true
provides_tools:
  - nails_scheduling
