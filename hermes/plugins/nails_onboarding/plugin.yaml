name: nails-onboarding
version: 0.5.0
description: Restricted onboarding bridge from the Nails Hermes profile to the local Booking API
requires_env:
  - name: NAILS_INTERNAL_API_KEY
    description: Internal authentication key for the local Nails Booking API
    secret: true
provides_tools:
  - nails_onboarding
