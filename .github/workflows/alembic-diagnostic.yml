name: Alembic diagnostic

on:
  pull_request:

permissions:
  contents: read

jobs:
  alembic:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: nails_test
          POSTGRES_USER: nails_app
          POSTGRES_PASSWORD: nails_test
        ports:
          - 55432:5432
        options: >-
          --health-cmd "pg_isready -U nails_app -d nails_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    defaults:
      run:
        working-directory: backend
    env:
      APP_TIMEZONE: Europe/Berlin
      INTERNAL_API_KEY: test-only-internal-api-key-0000000000000000
      DATABASE_URL: postgresql+psycopg://nails_app:nails_test@127.0.0.1:55432/nails_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install --quiet -e '.[dev]'
      - name: Capture migration output
        id: migrate
        continue-on-error: true
        shell: bash
        run: alembic upgrade head > /tmp/alembic.log 2>&1
      - name: Upload migration log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: alembic-diagnostic-log
          path: /tmp/alembic.log
      - name: Fail when migration failed
        if: steps.migrate.outcome == 'failure'
        run: exit 1
